{
  "name": "@agent-platform/escrow-contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "forge build",
    "check-types": "forge build",
    "test": "forge test -v"
  }
}
