{
  "name": "@agent-platform/qwen-image-to-code-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "mastra dev",
    "dev:platform": "node --env-file-if-exists=.env --watch src/main.ts",
    "generate": "node --env-file-if-exists=.env src/cli.ts",
    "build": "mastra build && tsc -p tsconfig.build.json",
    "start": "mastra start",
    "start:platform": "node --env-file-if-exists=.env dist/main.js",
    "check-types": "tsc --noEmit",
    "test": "node --test tests/*.test.ts"
  },
  "dependencies": {
    "@agent-platform/agent-runtime": "workspace:*",
    "@agent-platform/image-to-code-core": "workspace:*",
    "@mastra/core": "^1.53.0"
  },
  "devDependencies": {
    "@agent-platform/config": "workspace:*",
    "@types/node": "catalog:",
    "mastra": "^1.0.0",
    "typescript": "catalog:"
  }
}
